const createDonatorValidator = require('./create_donator-validator');
const editDonatorValidator = require('./edit_donator-validator');

module.exports = {
    createDonatorValidator,
    editDonatorValidator
};
