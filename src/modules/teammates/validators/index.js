const createTeammateValidator = require('./create_teammate-validator');
const editTeammateValidator = require('./edit_teammate-validator');

module.exports = {
    createTeammateValidator,
    editTeammateValidator
};
